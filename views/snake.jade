extends layout

block content
  head
    script(src=scripts[0])
    script(src=scripts[1])
  body
    .main-header
      h1 Snake
      p This was mostly made as an exercise to refamiliarize myself with Javascript after not having used it for a couple months. Who doesn't like Snake?
      p Click to choose your difficulty then use the arrow keys to move.
    .main-body
      .canvas-container
        canvas#canvas.canvas(width='640', height='480')
      script(src=scripts[2])
