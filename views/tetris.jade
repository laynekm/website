extends layout

block content
  head
    script(src=scripts[0])
    script(src=scripts[1])
  body
    .main-header
      h1 Tetris
      p Use the left/right keys to move, the up/down keys to rotate, and space to drop faster.
    .main-body
      .canvas-container
        canvas#canvas.canvas(width='440', height='440')
      script(src=scripts[2])
