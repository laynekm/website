extends layout

block content
  head
    script(src=scripts[0])
    script(src=scripts[1])
  body
    .main-header
      h1 Tetris
      p The classic Soviet puzzle game. A work in progress.
    .main-body
      .canvas-container
        canvas#canvas.canvas(width='640', height='480')
      script(src=scripts[2])
